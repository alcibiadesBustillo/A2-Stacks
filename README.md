# A2-Stacks: Starter Code – 2048 Merge Engine (1D)

Este repositorio contiene la estructura inicial para la asignación 
**2048 – Merge 1D usando `std::stack`** del curso de **Estructuras de Datos**.

---

## Instrucciones

- Implementa la función `merge_2048` en el archivo `main.cpp`.
- Usa **`std::stack<int>`** de la librería estándar (STL).
- No incluyas `main()`.
- No uses entrada/salida (`cin`, `cout`).
- No modifiques la firma de la función requerida.
- No utilices clases.

---

## Función a implementar

```cpp
std::vector<int> merge_2048(const std::vector<int>& nums);

